# DataClean Pro  
A lightweight and modular data‑cleaning engine designed to validate, sanitize, and transform CSV datasets.  
This tool helps ensure data quality by detecting invalid values, converting data types, removing incomplete rows, and generating detailed error reports.

---

## 🚀 Features

### ✔ Data Cleaning
- Remove rows with missing or invalid values  
- Normalize and convert data types (int, float, date)  
- Detect out-of-range or malformed fields  

### ✔ Validation Engine
- Custom validation rules for each column  
- Pluggable architecture for adding new validators  
- Clear separation between validation and cleaning logic  

### ✔ Reporting
- Generate a cleaned CSV file  
- Produce an error report listing invalid rows and reasons  
- Optional export to SQLite for long‑term storage  

### ✔ CLI Execution
Run the entire cleaning pipeline with a single command.

---

## 📁 Project Structure

